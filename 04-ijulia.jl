# Installing and working with IJulia

println("IJulia")
println("======")
println()
println("""The `IJulia` package supports using Jupyter notebooks with a
Julia backend.\n

To use `IJulia`, begin by adding the `IJulia` package to the 
current environment.\n

After adding the `IJulia` package, execute `using IJulia`.\n

Finaly, after adding the `IJulia` package, execute the function,
`notebook()`.\n

After executing the `notebook()` function, Julia will prompt you 
to install the Jupyter notebook using `conda`.\n

Once Julia installs the Jupyter notebook using `conda`, Jupyter
will add a new tab to the browser with the Jupyter notebook.
""")
